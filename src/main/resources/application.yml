server:
  port: 8090

zuul:
  routes:
    data:
      path: /api/**
      sensitiveHeaders: Cookie
      serviceId: data
      
data:
  ribbon:
      listOfServers: http://localhost:9090/

ribbon:
  eureka:
   enabled: false

management:
  security:
    enabled: false

security:
  basic:
    enabled: false

       